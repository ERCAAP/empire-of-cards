namespace EmpireOfCards.Core.StateMachine.TurnStates
{
    /// <summary>
    /// Documents why the deprecated phase files still exist in this folder.
    /// Unity keeps the legacy files around to preserve their meta identities
    /// while TurnManager owns the live phase flow.
    /// </summary>
    internal static class LegacyTurnStateShim
    {
    }
}
